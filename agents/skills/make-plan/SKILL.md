---
name: make-plan
description: Use any time user asks to make a plan or a spec
---

Store plans under ~/project-plans/<project name>/<plan directory>/. Write plans as self-contained HTML suitable for mobile.
Plans are synchronized across devices and backed up automatically.

If user asks to create an isolated prototype (not based on the source of the project), store the prototype near the plan.
